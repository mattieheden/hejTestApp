#!/bin/bash

# Changeable variables
resourceGroup=hejTestRG
port=3000

# Create a new resource group
az group create --name $resourceGroup --location northeurope

# Deploy ARM template
az deployment group create --resource-group $resourceGroup --template-file arm-deploy.json --parameters adminUsername=azureuser password=hejloggain123! appCustomData=@app-init.sh reverseProxyCustomData=@reverse-proxy-init.sh 
