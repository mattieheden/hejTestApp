#!/bin/bash

# Changeable variables
resourceGroup=hejTestRG
port=3000

# Create a new resource group
az group create --name $resourceGroup --location northeurope

# Deploy ARM template
az deployment group create --resource-group $resourceGroup --template-file arm-deploy.json --parameters adminUser=azureuser password=hejloggain123! customData=@app-init.sh customData=@reverse-proxy-init.sh 
